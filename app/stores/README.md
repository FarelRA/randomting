# Pinia Stores

This directory will contain Pinia stores for:

- `auth.ts` — user authentication state
- `history.ts` — tool usage history
- `presets.ts` — saved tool presets
- `theme.ts` — theme preferences
